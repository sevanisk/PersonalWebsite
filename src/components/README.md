# COMPONENTS

Reusable presentational UI components that can be used across features and pages. Typically small, self‑contained pieces such as buttons, form fields, cards, or layout helpers.

Components should be generic and accept props to alter behavior. Complex, feature‑specific components belong inside the corresponding directory under `features`.
