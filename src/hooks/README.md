# HOOKS

Custom React hooks encapsulating reusable stateful logic or effects. May be used by multiple components or features. Examples include:

- `useFetch` for data fetching
- `useDebounce` for input handling
- `useAuth` wrapping authentication logic

Should not render UI directly.
