# FEATURES

Groups together related functionality. Each subfolder typically represents a distinct feature or section of the app (e.g. `checkout`, `user-profile`, `blog`).

Features may contain components, hooks, slices, tests, and other code tightly coupled to that feature.
