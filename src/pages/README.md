# PAGES

Individual screens or pages in the application. Each file corresponds to a route defined in the router.

Pages compose layouts, features, and other components to render complete views. Keep page-specific state, effects, and navigation logic here; reusable UI should live in `components` or `features`.