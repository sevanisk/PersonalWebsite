# LAYOUTS

Layout components that define the overall page structure for different sections of the site. Wraps pages and provides shared UI elements such as headers, footers, navbars, or navigation bars.

Examples:

- `MainLayout.jsx` – common frame used by public-facing pages
- `AuthLayout.jsx` – wrapper for login/registration screens

Pages import and render a layout component to ensure consistent styling and structure across routes.
