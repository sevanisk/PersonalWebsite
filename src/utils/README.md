# UTILS

Helper functions and shared utilities used across the application. This directory is for pure JavaScript/TypeScript functions that do not depend on React components or application state. Keep reusable logic here such as:

- Date/number/string formatters
- Generic validation helpers
- Math utilities
- Anything that may be imported by multiple parts of the codebase

No React-specific code in this folder.
