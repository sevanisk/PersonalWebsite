# CONTEXT

React Context providers and associated hooks for global state management. This folder stores files like `AuthContext.js`, `ThemeContext.js`, and custom hooks that wrap `useContext`.

Used for data that needs to be accessible throughout the component tree (authentication info, theme settings, user preferences, etc.).

Export provider components and convenience hooks (`useAuth`, `useTheme`, etc.) from this directory.
